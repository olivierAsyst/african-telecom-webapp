{% extends 'base.html.twig' %}

{% block title %}Contact - {{ parent() }}{% endblock %}

{% block body %}
<!-- Hero Section -->
<section class="bg-gradient-to-br from-black via-gray-900 to-black text-white py-16">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <h1 class="text-4xl md:text-5xl font-bold mb-6 bg-gradient-to-r from-white to-gray-300 bg-clip-text text-transparent">
            Contactez-nous
        </h1>
        <p class="text-xl text-gray-300 max-w-3xl mx-auto">
            Nous sommes là pour vous accompagner dans votre transformation digitale
        </p>
    </div>
</section>

<!-- Contact Info -->
<section class="py-16 bg-gradient-to-b from-white to-gray-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
            {% for info in contactInfo %}
            <div class="bg-white rounded-lg shadow-lg p-6 text-center border border-gray-100 hover:shadow-xl transition duration-300">
                <div class="text-4xl mb-4">{{ info.icon }}</div>
                <h3 class="text-lg font-semibold text-black mb-2">{{ info.type }}</h3>
                <p class="text-gray-600">{{ info.value }}</p>
            </div>
            {% endfor %}
        </div>
    </div>
</section>

<!-- Contact Form & Map -->
<section class="py-16 bg-gradient-to-b from-gray-50 to-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-12">
            <!-- Contact Form -->
            <div class="bg-white rounded-lg shadow-lg p-8 border border-gray-100">
                <h2 class="text-2xl font-bold text-black mb-6">Envoyez-nous un message</h2>
                <form class="space-y-6">
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                        <div>
                            <label for="firstName" class="block text-sm font-medium text-gray-700 mb-2">Prénom</label>
                            <input type="text" id="firstName" name="firstName" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-gray-500 focus:border-transparent transition duration-300" required>
                        </div>
                        <div>
                            <label for="lastName" class="block text-sm font-medium text-gray-700 mb-2">Nom</label>
                            <input type="text" id="lastName" name="lastName" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-gray-500 focus:border-transparent transition duration-300" required>
                        </div>
                    </div>

                    <div>
                        <label for="email" class="block text-sm font-medium text-gray-700 mb-2">Email</label>
                        <input type="email" id="email" name="email" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-gray-500 focus:border-transparent transition duration-300" required>
                    </div>

                    <div>
                        <label for="phone" class="block text-sm font-medium text-gray-700 mb-2">Téléphone</label>
                        <input type="tel" id="phone" name="phone" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-gray-500 focus:border-transparent transition duration-300">
                    </div>

                    <div>
                        <label for="service" class="block text-sm font-medium text-gray-700 mb-2">Service souhaité</label>
                        <select id="service" name="service" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-gray-500 focus:border-transparent transition duration-300">
                            <option value="">Sélectionnez un service</option>
                            {% for service in services %}
                            <option value="{{ service }}">{{ service }}</option>
                            {% endfor %}
                        </select>
                    </div>

                    <div>
                        <label for="message" class="block text-sm font-medium text-gray-700 mb-2">Message</label>
                        <textarea id="message" name="message" rows="5" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-gray-500 focus:border-transparent transition duration-300" placeholder="Décrivez votre projet ou vos besoins..." required></textarea>
                    </div>

                    <button type="submit" class="w-full bg-gradient-to-r from-black to-gray-800 hover:from-gray-800 hover:to-black text-white px-6 py-3 rounded-lg font-semibold transition duration-300 transform hover:scale-105">
                        Envoyer le message
                    </button>
                </form>
            </div>

            <!-- Company Info -->
            <div class="space-y-8">
                <div class="bg-gradient-to-br from-black to-gray-900 rounded-lg p-8 text-white">
                    <h3 class="text-2xl font-bold mb-6">AFRICAN TELECOM</h3>
                    <p class="text-gray-300 mb-6">
                        Startup informatique spécialisée dans les services technologiques à forte valeur ajoutée,
                        basée en République Démocratique du Congo depuis 2021.
                    </p>

                    <div class="space-y-4">
                        <div class="flex items-center">
                            <div class="text-2xl mr-4">🏢</div>
                            <div>
                                <h4 class="font-semibold">Siège social</h4>
                                <p class="text-gray-300 text-sm">Uvira, République Démocratique du Congo</p>
                            </div>
                        </div>

                        <div class="flex items-center">
                            <div class="text-2xl mr-4">📅</div>
                            <div>
                                <h4 class="font-semibold">Fondée en</h4>
                                <p class="text-gray-300 text-sm">21 septembre 2021</p>
                            </div>
                        </div>

                        <div class="flex items-center">
                            <div class="text-2xl mr-4">🎯</div>
                            <div>
                                <h4 class="font-semibold">Spécialisation</h4>
                                <p class="text-gray-300 text-sm">Technologies de l'information</p>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="bg-white rounded-lg shadow-lg p-8 border border-gray-100">
                    <h3 class="text-xl font-bold text-black mb-4">Pourquoi nous choisir ?</h3>
                    <ul class="space-y-3">
                        <li class="flex items-start">
                            <div class="text-green-500 mr-3 mt-1">✓</div>
                            <span class="text-gray-600">Expertise locale en technologies</span>
                        </li>
                        <li class="flex items-start">
                            <div class="text-green-500 mr-3 mt-1">✓</div>
                            <span class="text-gray-600">Formation et accompagnement personnalisés</span>
                        </li>
                        <li class="flex items-start">
                            <div class="text-green-500 mr-3 mt-1">✓</div>
                            <span class="text-gray-600">Support technique 24/7</span>
                        </li>
                        <li class="flex items-start">
                            <div class="text-green-500 mr-3 mt-1">✓</div>
                            <span class="text-gray-600">Solutions adaptées aux jeunes entrepreneurs</span>
                        </li>
                        <li class="flex items-start">
                            <div class="text-green-500 mr-3 mt-1">✓</div>
                            <span class="text-gray-600">Incubation de startups informatiques</span>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Services Overview -->
<section class="py-16 bg-gradient-to-r from-gray-900 via-black to-gray-900 text-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-12">
            <h2 class="text-3xl md:text-4xl font-bold mb-6">Nos Services</h2>
            <p class="text-gray-300 text-lg max-w-2xl mx-auto">
                Des solutions complètes pour votre transformation digitale
            </p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            {% for service in services %}
            <div class="bg-gradient-to-br from-gray-800 to-black rounded-lg p-6 text-center hover:transform hover:scale-105 transition duration-300">
                <div class="text-3xl mb-4">
                    {% if 'Formation' in service %}🎓
                    {% elseif 'Support' in service %}💻
                    {% elseif 'Sécurisation' in service %}🔒
                    {% elseif 'Accompagnement' in service %}🚀
                    {% elseif 'Vente' in service %}📱
                    {% else %}🔧{% endif %}
                </div>
                <h3 class="text-lg font-semibold mb-2">{{ service }}</h3>
            </div>
            {% endfor %}
        </div>
    </div>
</section>

<!-- CTA Section -->
<section class="py-16 bg-gradient-to-b from-white to-gray-50">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <h2 class="text-3xl md:text-4xl font-bold text-black mb-6">Prêt à démarrer votre projet ?</h2>
        <p class="text-xl text-gray-600 mb-8">
            Contactez-nous dès aujourd'hui pour discuter de vos besoins technologiques
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
            <a href="tel:+243XXXXXXX" class="bg-gradient-to-r from-black to-gray-800 hover:from-gray-800 hover:to-black text-white px-8 py-3 rounded-lg font-semibold transition duration-300 transform hover:scale-105">
                📞 Appelez-nous
            </a>
            <a href="mailto:contact@africantelecom.cd" class="border-2 border-black text-black hover:bg-black hover:text-white px-8 py-3 rounded-lg font-semibold transition duration-300">
                ✉️ Envoyez un email
            </a>
        </div>
    </div>
</section>
{% endblock %}
